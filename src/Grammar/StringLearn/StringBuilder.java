package Grammar.StringLearn;

/**
 * JDK 1.5之后
 *
 * 单线程使用
 *
 * 一个可变的字符序列。 此类提供与StringBuffer的API，但不保证同步。
 * 此类设计用作简易替换为StringBuffer在正在使用由单个线程字符串缓冲区的地方（如通常是这种情况）。
 * 在可能的情况下，建议使用这个类别优先于StringBuffer ，因为它在大多数实现中将更快。
 *
 * StringBuffer 线程同步  多线程
 * StringBuilder 线程不同步 单线程 多线程（自己加锁）
 *
 * todo： 方法和 StringBuffer 一样
 *
 */
class StringBuilderCode {
    public static void main(String[] args) {

    }
}
